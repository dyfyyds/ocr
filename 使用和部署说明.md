# 智能项目管理系统 — 使用和部署说明

## 一、项目概述

本系统是一个面向企业项目全生命周期管理的 Web 应用，核心功能包括：

- **OCR 智能识别**：基于 PaddleOCR + LLM 的合同/发票/汇款单自动识别与字段提取
- **项目管理**：项目注册、合同上传、版本比对、立项审批、结项归档
- **财务管理**：发票管理、收付款管理、项目费用台账
- **系统管理**：用户管理、数据字典、系统配置、审计日志

**技术栈**：FastAPI (Python 3.12) + Vue 3 + MySQL 8.0 + Docker Compose

---

## 二、环境要求

| 依赖 | 最低版本 | 说明 |
|------|---------|------|
| Docker | 20.10+ | 容器运行时 |
| Docker Compose | 2.0+ | 服务编排 |
| Git | 2.30+ | 代码拉取 |
| 磁盘空间 | ≥ 5 GB | 含 PaddleOCR 模型缓存 |

> **Windows 用户**：推荐使用 Docker Desktop，确保已启用 WSL 2 后端。

---

## 三、快速部署（Docker Compose）

### 3.1 克隆代码

```bash
git clone https://github.com/dyfyyds/OCR-docker.git
cd OCR-docker
```

### 3.2 配置环境变量

复制环境变量模板并按需修改：

```bash
cp .env.example .env
```

编辑 `.env`，关键配置项说明：

```dotenv
# ---- MySQL ----
MYSQL_ROOT_PASSWORD=pm_root_2026     # root 密码
MYSQL_DATABASE=project_management     # 数据库名
MYSQL_USER=pm_user                    # 应用数据库用户
MYSQL_PASSWORD=pm_user_2026           # 应用数据库密码
MYSQL_PORT=3306                       # 宿主机 MySQL 端口

# ---- 后端 ----
BACKEND_PORT=8080                     # 宿主机后端端口
JWT_SECRET=pm-jwt-secret-key-change-in-production-2026   # JWT 密钥（生产环境务必更换！）

# ---- OCR / LLM ----
OCR_ENGINE=paddleocr                  # OCR 引擎
LLM_API_URL=https://token-plan-cn.xiaomimimo.com/v1/chat/completions
LLM_API_KEY=your-api-key-here         # LLM API Key
LLM_MODEL=mimo-v2.5-pro               # LLM 模型名

# ---- 前端 ----
FRONTEND_PORT=5173                    # 宿主机前端端口（Vite HMR）
NGINX_PORT=80                         # 宿主机 Nginx 入口端口
```

> **端口冲突**：如果默认端口被占用，修改 `MYSQL_PORT`、`BACKEND_PORT`、`FRONTEND_PORT`、`NGINX_PORT` 即可。

### 3.3 创建 PaddleOCR 模型缓存卷

```bash
docker volume create paddlex-models
```

> 首次启动时 PaddleOCR 会自动下载模型（约 1.5 GB），后续启动将使用缓存，无需重复下载。

### 3.4 启动服务

```bash
docker-compose up -d --build
```

首次启动会自动：
- 拉取 MySQL 8.0、Nginx Alpine 镜像
- 构建后端（安装 Python 依赖 + PaddleOCR）和前端镜像
- 执行 `mysql/init.sql` 初始化数据库（建表 + 种子数据）
- 启动 4 个服务并等待健康检查通过

### 3.5 验证部署

```bash
# 检查所有容器状态
docker-compose ps

# 预期输出：4 个服务均为 healthy / running
# pm-mysql     | healthy
# pm-backend   | healthy
# pm-frontend  | running
# pm-nginx     | healthy
```

访问系统：

| 地址 | 用途 |
|------|------|
| `http://localhost` | Nginx 入口（推荐） |
| `http://localhost:5173` | Vite 开发服务器（HMR） |
| `http://localhost:8080/docs` | Swagger API 文档 |
| `http://localhost:8080/api/health` | 后端健康检查 |

---

## 四、开发模式

开发模式下，后端和前端源码通过 volume 挂载到容器内，修改代码后自动热重载，无需重新构建镜像。

### 4.1 查看日志

```bash
# 实时查看所有服务日志
docker-compose logs -f

# 只看后端日志
docker-compose logs -f backend

# 只看 MySQL 日志
docker-compose logs -f mysql
```

### 4.2 进入容器调试

```bash
# 进入后端容器
docker-compose exec backend bash

# 进入 MySQL 容器
docker-compose exec mysql mysql -u pm_user -ppm_user_2026 project_management

# 进入前端容器
docker-compose exec frontend sh
```

### 4.3 常用运维命令

```bash
# 停止所有服务（保留数据）
docker-compose stop

# 停止并删除容器（保留数据卷）
docker-compose down

# 停止并删除容器 + 数据卷（⚠️ 会清除 MySQL 数据）
docker-compose down -v

# 重新构建某个服务
docker-compose build backend
docker-compose up -d backend

# 重建全部
docker-compose up -d --build
```

---

## 五、默认账号

系统初始化时自动创建以下演示账号，密码均为 `123456`：

| 用户名 | 角色 | 权限范围 |
|--------|------|---------|
| `admin` | 系统管理员 | 全部功能（用户管理、字典、配置、审计） |
| `business` | 商务经理 | 项目注册、合同上传、OCR 识别、项目查询 |
| `finance` | 财务总监 | 发票管理、收付款管理、费用台账、OCR 识别 |
| `pm` | 项目经理 | 项目详情、合同上传、OCR 识别、进度跟踪 |

> **首次登录后请立即修改密码**（设置 → 修改密码）。

---

## 六、功能使用指南

### 6.1 登录系统

1. 浏览器访问 `http://localhost`（或 `http://localhost:18088`，取决于 `.env` 中的 `NGINX_PORT`）
2. 输入用户名和密码，点击登录
3. 登录成功后自动跳转到工作台首页

### 6.2 OCR 文档识别

1. 左侧菜单 → **OCR 识别**
2. 上传文件（支持 `.docx`、`.pdf`、`.jpg`、`.png`，最大 20 MB）
3. 系统自动执行：
   - PaddleOCR 文字识别
   - 正则 + NLP 三路字段提取（合同 / 发票 / 汇款）
   - LLM 智能补全（可选）
4. 识别结果自动填充到对应表单字段

### 6.3 项目管理流程

```
注册项目 → 上传合同 → OCR 识别 → 版本比对 → 提交审批 → 审批通过 → 项目执行 → 结项归档
```

1. **注册项目**：填写项目名称、类型、金额、甲乙方等基本信息
2. **上传合同**：支持 Word (.docx) 和 PDF 格式，系统自动 OCR 识别关键条款
3. **版本比对**：系统比对 OCR 识别结果与手工填写字段的差异，标记不一致项
4. **提交审批**：确认无误后提交至管理员审批
5. **审批**：管理员审核通过/驳回，审批记录自动写入审计日志
6. **结项**：项目完成后提交结项申请，归档全部资料

### 6.4 发票与收付款

- **发票管理**：上传发票图片，OCR 自动识别发票号码、税率、金额等
- **收付款管理**：记录每笔收付款，支持银行转账、支票、现金等支付方式
- **费用台账**：汇总项目所有费用（发票 + 收付款 + 其他支出）

### 6.5 系统管理（admin 角色）

- **用户管理**：查看/编辑/禁用用户账号
- **数据字典**：维护项目类型、发票类型、支付方式等枚举值
- **系统配置**：OCR 引擎、LLM 接口、文件大小限制等参数在线调整
- **审计日志**：查看所有审批操作的完整记录

---

## 七、项目结构

```
OCR-docker/
├── docker-compose.yml          # Docker 服务编排
├── .env / .env.example         # 环境变量
├── backend/                    # FastAPI 后端
│   ├── Dockerfile
│   ├── main.py                 # 应用入口
│   ├── requirements.txt        # Python 依赖
│   ├── app/
│   │   ├── api/                # API 路由（auth、projects、invoices、ocr 等）
│   │   ├── models/             # SQLAlchemy ORM 模型
│   │   ├── schemas/            # Pydantic 请求/响应模型
│   │   ├── services/           # 业务逻辑层
│   │   ├── core/               # OCR / NLP / LLM 引擎
│   │   ├── db/                 # 数据库连接
│   │   ├── middleware/          # 中间件（异常处理、响应包装）
│   │   └── utils/              # 工具函数
│   └── uploads/                # 上传文件存储
├── frontend/                   # Vue 3 前端
│   ├── Dockerfile
│   ├── spa.html                # Vite 入口
│   ├── src/
│   │   ├── views/              # 页面组件（Login、Dashboard、Projects 等）
│   │   ├── api/                # API 调用层
│   │   ├── router/             # Vue Router 路由
│   │   └── store/              # Pinia 状态管理
│   └── index.html              # 原型页面（独立 SPA）
├── nginx/
│   └── nginx.conf              # Nginx 反向代理配置
├── mysql/
│   └── init.sql                # 数据库初始化脚本
└── uploads/                    # 文件上传目录（contracts/invoices/payments/reports）
```

---

## 八、API 接口概览

后端提供 RESTful API，所有接口统一返回格式：

```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

| 模块 | 路径前缀 | 主要接口 |
|------|---------|---------|
| 认证 | `/api/auth` | 登录、刷新 Token、获取当前用户、修改密码 |
| 用户 | `/api/users` | 用户 CRUD |
| 项目 | `/api/projects` | 项目注册、编辑、合同上传、版本比对、提交审批、审批 |
| 发票 | `/api/projects` | 发票 CRUD、OCR 识别 |
| 收付款 | `/api/projects` | 收付款 CRUD |
| 费用 | `/api/projects` | 项目费用管理 |
| 结项 | `/api/projects` | 结项申请、结项审批 |
| 工作台 | `/api/dashboard` | 统计数据 |
| 字典 | `/api/dict` | 数据字典管理 |
| 配置 | `/api/config` | 系统配置管理 |
| 上传 | `/api/uploads` | 文件上传/下载 |
| OCR | `/api/ocr` | 独立 OCR 识别（不绑定项目） |
| 健康检查 | `/api/health` | 服务状态 |

完整 API 文档：`http://localhost:8080/docs`（Swagger UI）

---

## 九、数据库说明

- **数据库**：MySQL 8.0，字符集 `utf8mb4`
- **初始化**：首次启动时自动执行 `mysql/init.sql`，建表并插入种子数据
- **持久化**：数据存储在 `mysql-data/` 目录（Docker volume 映射）
- **备份**：

```bash
# 导出数据库
docker-compose exec mysql mysqldump -u root -ppm_root_2026 project_management > backup.sql

# 导入数据库
docker-compose exec -T mysql mysql -u root -ppm_root_2026 project_management < backup.sql
```

---

## 十、常见问题

### Q1：首次启动很慢？

首次启动需要下载 PaddleOCR 模型（约 1.5 GB），后续启动会使用 `paddlex-models` 卷缓存，速度正常。

### Q2：端口被占用怎么办？

修改 `.env` 中对应的端口变量：

```dotenv
MYSQL_PORT=13306
BACKEND_PORT=18080
FRONTEND_PORT=15173
NGINX_PORT=18088
```

然后重启：`docker-compose up -d`

### Q3：OCR 识别结果不准确？

- 确保上传图片清晰、分辨率足够
- PDF 文件优先使用文字版（非扫描件）
- 可在「系统配置」中启用 LLM 辅助提取

### Q4：如何查看 API 接口文档？

访问 `http://localhost:8080/docs`，Swagger UI 提供完整的接口测试功能。

### Q5：如何重置数据库？

```bash
docker-compose down -v           # 删除容器和数据卷
rm -rf mysql-data/               # 清除本地数据目录
docker volume create paddlex-models
docker-compose up -d --build     # 重新启动（自动初始化）
```

### Q6：生产环境部署注意事项

1. **修改 JWT_SECRET**：使用高强度随机字符串
2. **修改数据库密码**：更换所有默认密码
3. **关闭 CORS 通配**：限制为实际域名
4. **启用 HTTPS**：在 Nginx 层配置 SSL 证书
5. **关闭调试模式**：移除 uvicorn 的 `--reload` 参数
6. **配置日志持久化**：挂载日志目录到宿主机
